-- ============================================================
-- 清理旧的新闻相关表和定时任务
-- ============================================================
-- 说明：
-- 1. 新闻系统已重构为直接使用 crawler 采集数据到 hot_news、
--    today_hotspot、financial_calendar、community_posts 表
-- 2. 此迁移文件用于清理不再使用的旧表和定时任务
-- ============================================================

-- ============================================
-- 1. 取消旧的定时任务
-- ============================================
-- 取消 fetch-galaxy-news 定时任务（如果存在）
SELECT cron.unschedule('fetch-galaxy-news-every-30min');

-- ============================================
-- 2. 删除旧的新闻表
-- ============================================
-- 删除 news 表（已被 hot_news 替代）
DROP TABLE IF EXISTS news CASCADE;

-- 删除 calendar_events 表（已被 financial_calendar 替代）
DROP TABLE IF EXISTS calendar_events CASCADE;

-- ============================================
-- 说明
-- ============================================
-- 已删除的内容：
-- | 对象 | 类型 | 说明 |
-- |------|------|------|
-- | news | 表 | 旧的新闻表，已被 hot_news 替代 |
-- | calendar_events | 表 | 旧的日历事件表，已被 financial_calendar 替代 |
-- | fetch-galaxy-news-every-30min | 定时任务 | 旧的新闻刷新任务，已不再需要 |
